syntax = "proto3";
package api.common;

option go_package = "wcs/api/common;common";
option swift_prefix = "Common";

// 通用计数器类型
enum CounterIDType {
  // @String(未知id)
  CIT_UNKNOWN = 0;
  // @String(会话消息id)
  CIT_CHAT_MESSAGE = 1;
  // @String(分配用户id)
  CIT_ASSIGN_USER = 2;
}